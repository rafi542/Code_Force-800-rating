#include <bits/stdc++.h>
using namespace std;
int main()
{
  long long int g, i, j, h, cn, n, k, t, f, a, m;
  long long int mid, left, right, s, sum, stop, sum1, start;
  long long sum2;
  vector<int>v;
  vector<int>v1;
  cin >> t;
  while(t--)
  {
    cin >> n;
    if(n%3 != 0)
      cout << "First" << endl;
    else
      cout << "Second" << endl;  
  }  
}  
