#include <bits/stdc++.h>
using namespace std;
int main()
{
  long long int  i, n, g, a, b, c, t;
  string s, y;
  cin >> s;
  cin >> y;
  reverse(y.begin(), y.end());
  if(s==y)
    cout << "YES" << endl;
  else
    cout << "NO" << endl;  
}  
